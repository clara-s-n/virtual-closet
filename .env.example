# Virtual Closet Environment Configuration
# Copy this file to .env in the root directory

# IMPORTANT: Generate a secure JWT secret for production
# Use: openssl rand -base64 32
JWT_SECRET=CHANGE-THIS-SECRET-IN-PRODUCTION-USE-ENV-FILE

# Optional: Override default PostgreSQL password
# POSTGRES_PASSWORD=your-secure-password
